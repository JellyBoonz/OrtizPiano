[build]
  command = "npm run build"
  publish = ".next"
  functions = "netlify/functions"

# Next.js handles routing internally, so we don't need catch-all redirects
# The Next.js runtime on Netlify will handle routing automatically